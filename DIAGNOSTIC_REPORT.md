# 🔍 ПОЛНАЯ ДИАГНОСТИКА СИСТЕМЫ СОБЫТИЙ

## 📊 ВИЗУАЛЬНАЯ СХЕМА ПОТОКА ДАННЫХ

```
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  EventsPage.tsx (строка 16)                                     │
│  └─> useEvents() hook                                           │
│      └─> getEvents() из events.service.ts                       │
│          └─> GET http://localhost:8000/api/events/              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API (FastAPI)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  /api/events/ (GET)                                             │
│  └─> backend/app/api/routers/events.py:61                      │
│      └─> search_events()                                        │
│          └─> EventService.search_events()                       │
│              └─> EventRepository.search()                       │
│                  └─> SELECT * FROM events                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (PostgreSQL)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  events table                                                   │
│  └─> Может быть ПУСТАЯ если скраперы еще не запустились        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔎 НАЙДЕННЫЕ ФАЙЛЫ

### Frontend:
1. **`frontend/src/pages/EventsPage.tsx`** (строка 228-230)
   - Показывает "Ничего не найдено" если `filteredEvents.length === 0`
   - Условие: `filteredEvents.length > 0 ? (карточки) : ("Ничего не найдено")`

2. **`frontend/src/hooks/useEvents.ts`** (строка 36)
   - Устанавливает `events` из `eventsRes.items`
   - Если API возвращает `[]`, то `events = []`

3. **`frontend/src/services/events.service.ts`** (строка 21)
   - Делает GET запрос к `/events/`
   - Ожидает массив `Event[]` в `response.data`
   - При ошибке возвращает `{ items: [], total: 0 }`

### Backend:
4. **`backend/app/api/routers/events.py`** (строка 61-100)
   - Endpoint: `GET /api/events/`
   - Возвращает `List[EventRead]` (массив)
   - При ошибке возвращает `[]`

5. **`backend/app/services/event_service.py`** (строка 55-88)
   - Вызывает `event_repo.search()`
   - Возвращает `[]` если БД пустая

6. **`backend/app/repositories/event_repository.py`** (строка 15-66)
   - Делает SQL запрос: `SELECT * FROM events`
   - Возвращает `[]` если нет записей

## 🐛 КОРНЕВАЯ ПРИЧИНА

**ПРОБЛЕМА**: БД пустая или скраперы еще не сохранили события

**ПОЧЕМУ ПОКАЗЫВАЕТСЯ "НИЧЕГО НЕ НАЙДЕНО"**:
1. API возвращает `[]` (пустой массив)
2. Frontend получает `[]`
3. `filteredEvents = []` (после фильтрации)
4. Условие `filteredEvents.length > 0` = `false`
5. Показывается блок "Ничего не найдено" (строка 228)

## ✅ РЕШЕНИЕ

1. Добавить fallback данные если БД пустая
2. Улучшить диагностику (логирование)
3. Показывать сообщение "События загружаются..." вместо "Ничего не найдено"
4. Добавить проверку что скраперы работают

