# 🛠️ Event Hub - Технологический стек

## 📱 Frontend

### Основные технологии:
- **React 18.3.1** - UI библиотека
- **TypeScript** - типизация
- **Vite 5.4.21** - сборщик и dev-сервер
- **React Router DOM** - маршрутизация
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации

### Дополнительные библиотеки:
- **react-i18next** - интернационализация (RU, EN, KZ)
- **axios** - HTTP клиент
- **lucide-react** - иконки
- **date-fns** - работа с датами

### Стилизация:
- **Tailwind CSS** - utility-first CSS framework
- Кастомные компоненты с темной темой (slate-900, slate-800)

---

## 🔧 Backend

### Основной фреймворк:
- **FastAPI** - современный Python веб-фреймворк
- **Python 3.9+** - язык программирования
- **Uvicorn** - ASGI сервер

### База данных:
- **PostgreSQL** - основная БД
- **SQLAlchemy 2.0** - ORM
- **asyncpg** - асинхронный драйвер PostgreSQL
- **Alembic** - миграции БД

### Аутентификация:
- **JWT** (JSON Web Tokens) - токены доступа
- **OAuth2PasswordRequestForm** - стандарт OAuth2
- **bcrypt** - хеширование паролей

### Парсинг данных:
- **httpx** - асинхронный HTTP клиент
- **BeautifulSoup4** - парсинг HTML
- **APScheduler** - планировщик задач (CRON)

### Дополнительно:
- **Pydantic** - валидация данных
- **python-multipart** - загрузка файлов

---

## 🐳 Docker

### Контейнеризация:
- **Docker** - контейнеризация приложений
- **Docker Compose** - оркестрация контейнеров
- **Backend Dockerfile** - образ для FastAPI приложения
- Сервисы: `backend`, `db` (PostgreSQL), `redis`

---

## 💾 База данных

### Основная БД:
- **PostgreSQL** - реляционная БД
- **Supabase** (опционально) - облачная PostgreSQL

### Структура:
- Таблицы: `users`, `events`, `teams`, `event_registrations`, и др.
- Асинхронные запросы через SQLAlchemy
- Миграции через Alembic

---

## 🔄 Redis

### Кэширование и сессии:
- **Redis 5.0.8** - in-memory хранилище
- **redis.asyncio** - асинхронный клиент
- Кэширование событий
- Онлайн статусы в чатах
- URL: `redis://redis:6379/0` (Docker) или `redis://localhost:6379/0` (локально)

---

## 📦 Деплой

### Frontend:
- **GitHub Pages** - статический хостинг
- **Vite build** - production сборка

### Backend:
- **Uvicorn** - production сервер
- **0.0.0.0:8000** - порт по умолчанию

---

## 🔌 API

### Endpoints:
- `GET /events/` - список событий
- `POST /events/scrape-now` - ручной парсинг
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход
- `GET /admin/*` - админ панель

### Формат:
- **REST API**
- **JSON** - формат данных
- **CORS** - настроен для localhost:3000, 3004, 5173

---

## 🎨 UI/UX

### Дизайн:
- **Apple-style** - минималистичный дизайн
- **Dark theme** - темная тема (slate-900)
- **Responsive** - адаптивная верстка
- **Gradient backgrounds** - градиентные фоны

### Компоненты:
- **LiquidEther** - анимированный фон (Three.js)
- **EventCard** - карточки событий
- **Modal** - модальные окна (Login/Register)

---

## 📊 Мониторинг и логирование

- **Python logging** - логирование на бэкенде
- **Console.log** - логирование на фронтенде
- **Error handling** - обработка ошибок

---

## 🚀 Запуск проекта

### Frontend:
```bash
cd frontend
npm install
npm run dev  # http://localhost:3000
```

### Backend:
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload  # http://localhost:8000
```

---

## 📝 Версии

- **Node.js**: 18+
- **Python**: 3.9+
- **PostgreSQL**: 14+
- **React**: 18.3.1
- **FastAPI**: последняя версия

